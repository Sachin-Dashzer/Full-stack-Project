import mongoose from 'mongoose';
import DB_NAME from '../constants.js'



const dbConnection = async () =>{

    try{

        const db = await

    }
    catch(err){
        console.log(err)
        process.exit(1)
    }
} 